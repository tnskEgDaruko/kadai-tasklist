<h1>タスクの一覧ページ</h1>

<% @tasks.each do |task| %>
  <ul>
    <li><%= link_to task.id, task %> : <%= task.content %> 優先度 &gt;<%= task.status %></li>
  </ul>
<% end %>

<%= link_to '新規タスク作成', new_task_path %>