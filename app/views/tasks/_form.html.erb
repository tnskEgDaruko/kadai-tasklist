<%= form_for(task) do |f| %>
  <%= f.label :content, 'タスク' %>
  <%= f.text_field :content %>
  
  <%= f.label :status, '優先度' %>
  <%= f.select :status, [["高","★★★"],["中","★★"],["低","★"]] %>
  
  <%= f.submit '保存' %>
<% end %>